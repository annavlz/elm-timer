# elm-timer
Small timer written in Elm


To run it:
* clone repo to a dir
* go to the dir
* `npm install`
* `elm package install -y`
* `grunt elm`
* `elm reactor`
* go to localhost:8000
* click on `index.html`
